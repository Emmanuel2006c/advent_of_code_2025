import time,re

def loadfile(file):
    with open(file) as f:
        


def solve(p)
    return p


time_start = time.perf_counter()
print(f"Solution. {solve(loadfile(day6.txt))}")
print(f"Thought for {time_start - time.perf_counter:.6f} Seconds")